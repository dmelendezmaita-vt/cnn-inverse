# Legacy Upload Bundle

An earlier allowlist-based packaging bundle existed during the April 7 GitHub-preparation pass. It is intentionally excluded from the current public tree, because the present repository should expose the canonical reproduction chain directly rather than preserving an older track-based export surface.

Historical workspace reference:

- `/projects/neuro-collab/code/fhn_dnn-1-implementation-in-pytorch/data/important_notes/github_upload_bundle_20260407/`
